#!/usr/bin/env python3
'''This is the 0-add module'''


def add(a: float, b: float) -> float:
    """Adds two floats and returns
        Args:
        a(float): first number.
        b(float): secomd number.

        Returns:
        (float): the sume of the two numbers."""
    return a + b
